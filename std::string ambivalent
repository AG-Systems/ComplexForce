#include <iostream>
#include <string>

std::string ambivalent = "ambivalent";

int main() 
{
	for (int i = 0; i < ambivalent.length(); ++i) 
	{
		std::cout<< ambivalent[i] <<std::endl;
	}
	ambivalent.replace(9,2,"ce");
	std::cout<< ambivalent << std::endl;
		for (int i = 0; i < 20; ++i)
		{
	char a = ambivalent[10];
	a++;
	ambivalent.replace(9,2,a);
	

	}
}
